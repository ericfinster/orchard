/**
  * JsModuleEntry.scala - Javascript Implementation of ModuleEntries
  * 
  * @author Eric Finster
  * @version 0.1 
  */

package orchard.js

import scala.scalajs._
import org.scalajs.jquery._

import orchard.core.checker._

// class JsModule(name : String, entries : Vector[ModuleEntry], val moduleDiv : JQuery) extends Module(name, entries) {

//   // Now, the idea is that we store the user interface setup here.  Moreover, we should have
//   // a javascript class which we instantiate and pass around as a parameter

//   // The main thing is that we need an entry which points to the div where this guy stores his children.

//   override def copy(name : String = this.name, entries : Vector[ModuleEntry] = this.entries) : Module =
//     new JsModule(name, entries, moduleDiv)

// }


